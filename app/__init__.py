"""Core package initializer for the bot application."""

# Ensures the "app" directory is treated as a Python package so that
# absolute imports like ``import app.models`` work when running the test
# suite from the project root.
